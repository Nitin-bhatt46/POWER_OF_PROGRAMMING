for char in range(ord('a'), ord('z') + 1):
    # ord  provide unicode the character. 
    print(chr(char))
    # chr covert unicode into character.
# to print all in one line we use
   # print(chr(char), end = '  ')
